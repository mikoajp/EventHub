security:
    enable_authenticator_manager: true
    firewalls:
        main:
            lazy: true
            stateless: false
            security: false
    access_control:
        - { path: ^/, roles: PUBLIC_ACCESS }
