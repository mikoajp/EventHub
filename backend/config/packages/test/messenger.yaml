framework:
    messenger:
        failure_transport: null
        transports:
            async: 'in-memory://'
            high_priority: 'in-memory://'
            notifications: 'in-memory://'
            failed: 'in-memory://'
            sync: 'sync://'
        routing:
            'App\Message\*': sync
