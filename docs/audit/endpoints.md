# Endpoints returning entities directly

Scan result: none detected. Controllers format arrays or use Presenters.

Heuristics used: searched for `return $entity;` and `$this->json($entity)` in src/Controller.
